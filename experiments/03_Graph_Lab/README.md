
**The Graph is the authoritative representation of a MIOS instrument’s structural state. It owns the Modules belonging to the instrument and admits only Core-valid Connections between Modules belonging to that Graph.**

The authoritative representation of what the instrument is. With the Graph defined by modules inputted, it can allow the connections, endpoints, modules, to function within their assigned parameters.
